# GEOFlow 深度复审与完善记录

## 1. 结论

- 复审时间：2026-08-23（Asia/Shanghai）。
- 本轮完成了 Models 发布探针、可信代理、API 限流、依赖安全、生产镜像、后台无障碍语义、MCP 和真实 Docker/Chrome 回归。
- 应用代码和自动化测试已转绿；生产 Compose 未创建 PostgreSQL、Redis 或 RabbitMQ，继续连接 `../docker-helm.dofe.ai` 管理的外部服务。
- 当前环境尚不能正式放行：五个常驻服务、Web、数据库和迁移门禁已通过，但运行容器缺少 Models 公共探针的四项受保护配置，严格健康脚本按设计失败。

## 2. 深审发现与修复

| 发现 | 风险 | 完善结果 |
| --- | --- | --- |
| 可信代理允许伪造转发主机影响应用 URL | 链接、重定向和资源地址可能被污染 | 生产环境校验请求有效主机必须匹配 `APP_URL`，`/up` 保持容器健康检查可用 |
| Models Bearer 密钥可随 HTTP 地址发送 | 明文泄漏模型服务密钥 | 公共网关默认强制 HTTPS；同机 HTTP 验收要求专用开关和精确私网 `host:port` 白名单同时存在 |
| Models 探针只判断数组非空 | 无内容 Chat 或无效向量可能误过发布门禁 | 校验非空文本以及非空、有限数值 Embedding 向量 |
| 上游错误正文直接写入命令输出 | CI 日志可能泄漏 Provider 响应字段 | 命令仅输出净化后的状态与失败类别，不回显正文、密钥或上游主机 |
| 外部探针没有瞬态重试 | 连接抖动、429、5xx 会误阻断发布 | 仅对连接异常、429 和 5xx 执行两次受限退避；确定性 4xx 不重试 |
| API v1 没有统一请求频率边界 | 单令牌或单来源可放大资源消耗 | 所有 API v1 路由使用共享限流器，并按 Bearer 摘要和来源 IP 双层分区 |
| npm/Composer 依赖存在已知公告 | 前端与 PHP 供应链风险 | 升级锁文件；npm 和 Composer 安全审计均为 0 个公告 |
| 生产构建在线下载 `latest-stable` Composer | 构建不可复现且增加供应链入口 | 仅使用传入 Composer 镜像中的二进制，生产 vendor 阶段构建通过 |
| 核心后台静态标签仅依赖 `aria-label` | 点击可见标签无法聚焦，语义关联弱 | 文章、分析和 URL 导入的 20 个静态控件改为稳定 `id`/`for`；补齐展开关系和模型测试结果播报 |
| 无障碍扫描器漏查引用和数据行 | 空列表或失效 ARIA 关系可误过 | 增加 AI 模型与文章数据夹具，并检查空/重复 ID、`aria-controls`、`aria-expanded` 和标签文本 |
| Models 内部 HMAC 可通过 HTTP 发送 | 内部签名令牌可能明文泄漏 | 内部与公共网关共用 HTTPS 策略；固定本地 HTTP 同样要求专用开关和精确私网白名单 |
| Models 内部探针回显上游正文且缺少响应校验 | CI 日志泄漏、无效目录误过、瞬态 5xx 直接阻断 | 输出仅保留净化 HTTP 状态；严格校验 `list/total`；只重试连接异常、429 和 5xx |
| 健康脚本自行用 `grep/cut` 解析 dotenv | 带引号布尔值或已重建容器配置可能被误判 | 从运行中 app 容器读取 Compose 已解析环境，并从实际端口映射确定 Web 端口 |
| 预构建发布后使用了源码构建 Compose 做健康检查 | 验收对象与发布对象不一致，且常驻进程缺少渲染环境 | 健康脚本显式选择 prod/prebuilt；预构建发布固定使用 prebuilt，五个 PHP 服务接收统一环境 |
| queue、scheduler、Reverb 只检查容器 `running` | 主进程或外部 Redis/监听异常仍可能放行 | 两套生产 Compose 增加进程与依赖健康探针，发布门禁要求五个常驻服务全部 `healthy` |
| AI 模型弹窗只有 ARIA 外壳 | 键盘焦点可逃逸，Escape 与焦点恢复缺失 | 增加初始焦点、双向 Tab 陷阱、Escape、滚动锁和触发按钮焦点恢复 |

## 3. 自动化验证

| 验证项 | 结果 |
| --- | --- |
| 完整 PHPUnit | `1041 passed, 1 skipped, 8154 assertions` |
| 后台/Models 定向测试 | 无障碍相关 `52 passed / 388 assertions`；Models 与出站安全 `118 passed / 486 assertions` |
| Vite 生产构建 | 通过 |
| npm 官方安全审计 | `0 vulnerabilities` |
| Composer 锁文件安全审计 | `No security vulnerability advisories found` |
| Compose 外部依赖边界 | 通过，未定义 PostgreSQL/Redis/RabbitMQ 服务 |
| Laravel 数据库迁移 | 可连接且无待执行迁移 |
| 生产运行服务 | app、web、queue、scheduler、reverb 均为 `healthy`；queue 探针同时验证外部 Redis 连接 |

唯一跳过项是系统更新完整发布计划测试，该测试需要容器内 Git；其余测试全部通过。

## 4. 真实环境回归

生产入口使用 `http://127.0.0.1:18080`：

- 正常首页返回 `200`。
- 带伪造 `X-Forwarded-Host` 的首页请求返回 `400`，页面未输出攻击者主机。
- 相同伪造头访问 `/up` 返回 `200`，不会破坏容器健康检查。
- MCP `initialize`、`tools/list` 和 `geoflow.catalog` 全部通过；无令牌请求返回 `401`。
- Google Chrome 隔离环境检查桌面/移动首页与归档共 4 个页面/视口：全部 `200`、每页 1 个 H1、0 横向溢出、0 未命名操作、0 未命名控件、0 console 错误/警告、0 失败请求。
- AI 模型管理弹窗在 1440x1000 和 390x844 两个视口通过真实 Chromium 回归：无横向溢出；无障碍树暴露命名 dialog；打开后聚焦名称；Tab/Shift+Tab 不逃逸；Escape 关闭并恢复触发按钮焦点；0 console 错误/警告、0 页面异常、0 失败请求。
- 本会话没有 Chrome DevTools MCP，浏览器验收使用隔离 Google Chrome 的 Playwright/CDP 完成；没有附着用户日常浏览器配置。

## 5. 当前阻塞与运行态风险

严格发布脚本当前依次确认五个服务全部 `healthy`，并通过 HTTP、数据库和无待执行迁移检查，随后在 Models 公共网关处失败。最终运行态明确缺少：

- `MODELS_BASE_URL`
- `MODELS_API_KEY`
- `MODELS_CHAT_SMOKE_MODEL`
- `MODELS_EMBEDDING_SMOKE_MODEL`

这是受保护部署输入缺失，不应在仓库中补假值或绕过门禁。公共探针通过后，脚本还会继续要求内部 HMAC 与 MCP 协议冒烟通过。

正式放行前必须完成：

1. 生产优先配置 HTTPS `MODELS_BASE_URL`、服务密钥、Chat smoke 和 Embedding smoke 模型；若确需同机 HTTP，必须同时开启专用本地开关与精确私网白名单。
2. 配置 `MODELS_INTERNAL_BASE_URL` 和内部 HMAC 密钥，执行 `geoflow:models-internal-check`。
3. 更新 `.env.prod` 后清除并重建 Laravel 配置缓存，重启 app、queue、scheduler、reverb，确认文件配置与所有进程运行态一致。
4. 重新执行 `deploy-scripts/geoflow-healthcheck.sh`，要求 Models 公共、内部与 MCP 三组门禁全部通过。
5. 使用正式域名和有效生产 SSO 管理员完成后台弹窗键盘焦点、Escape 关闭、焦点恢复和完整管理业务浏览器回归。

## 6. 本轮提交

- `0a6649f` 阻断伪造转发主机覆盖应用地址
- `6959b30` 禁止模型网关通过明文地址传输密钥
- `9268ac1` 严格校验模型网关探针响应
- `85d74fb` 防止模型探针泄露上游错误正文
- `fe757d2` 为模型发布探针增加受限重试
- `6795558` 升级前端依赖修复高危漏洞
- `f81e805` 为 API 增加令牌与来源双层限流
- `8482585` 升级 PHP 依赖修复安全公告
- `ebbec90` 固定生产构建使用镜像内 Composer
- `cd2f96e` 完善后台控件无障碍语义关系
- `8a2e1b8` 允许本地模型网关并记录 Embedding 回归
- `72f6660` 修正本地模型网关双重放行契约
- `61f6930` 贯通本地模型网关部署安全配置
- `a61c399` 加固模型内部 HMAC 发布探针
- `204d561` 校准部署健康检查运行时配置
- `f8e43ac` 修正预构建发布健康检查栈
- `27a1eac` 强化生产运行服务健康门禁
- `453325d` 完善 AI 模型管理弹窗无障碍交互
