# 0821 后续推进记录

## 本轮完成

- `b6757f5`：修复 MCP 系统部署令牌调用写工具时无租户作用域仍触发 `ensureTaskInScope` 的问题。系统令牌继续表示跨租户部署权限，SSO 令牌才执行团队范围校验。
- `3c68dbb`：JSON-RPC `notifications/initialized` 无 `id` 时返回标准 204 空响应。
- `a504377`：models 管理端点改用专用 `MODELS_INTERNAL_API_SECRET`，兼容回退旧的 `INTERNAL_API_SECRET`；移除未被调用的 `MODELS_TEAM_ID` 配置，避免误导部署人员。
- `f69578e`：models 管理端点请求接入统一 `SafeOutboundHttpClient`，遵守 DNS/IP 校验、响应体上限和禁止任意重定向的出站安全边界。
- CI 增加 models 内部客户端/鉴权命令的语法检查和 MCP/models 聚焦测试。

## 当前调用边界

| 能力 | GEOFlow 配置 | models 路径 | 鉴权 |
| --- | --- | --- | --- |
| Chat | `MODELS_BASE_URL`、`MODELS_API_KEY` | `/v1/chat/completions` | 公共 API key |
| Embedding | `MODELS_BASE_URL`、`MODELS_API_KEY` | `/v1/embeddings` | 公共 API key |
| 模型目录管理 | `MODELS_INTERNAL_BASE_URL`、`MODELS_INTERNAL_API_SECRET` | `/internal/models` | 服务间 HMAC + `x-service-name` |

不要将 `/internal/*` 作为 Chat 或 Embedding Provider 地址；models 当前内部代理只覆盖管理面和特定内部能力。

## 验证结果

- `ModelsInternalClientTest` + `McpEndpointTest`：19 tests / 44 assertions 通过。
- `OutboundEntryPointArchitectureTest` + `ModelsInternalClientTest`：9 tests / 1687 assertions 通过。
- 新增 PHP 文件语法检查：通过。
- Compose 外部基础设施边界检查：通过。

完整项目测试集尚未全绿：当前工作区已有的葡萄牙语翻译缺项、后台页面/主题断言、GD 扩展缺失以及若干分发/AI 测试失败，最终因 128 MB 内存上限退出。上述失败不属于本轮 models/MCP 改动；CI 的全量 `php artisan test` 会继续阻止不完整版本合入。MCP 聚焦套件已在 PHP 8.4 SQLite 环境中完整通过（17 tests / 41 assertions）。

## CI 机执行

```bash
php artisan geoflow:models-internal-check
php artisan geoflow:models-internal-check --list
php artisan test --filter='ModelsInternalClientTest|McpEndpointTest'
```

部署仍只启动 GEOFlow 应用容器，PostgreSQL、Redis、RabbitMQ 由 `docker-helm.dofe.ai` 集中提供。
